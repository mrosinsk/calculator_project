pipeline {
    agent any


    stages {


        stage('Verify Branch') {
              steps {
                  echo "$BRANCH_NAME"
              }
        }

        stage("Hello") {
               steps {
                    echo "Hello World"
               }
        }


	}
}
